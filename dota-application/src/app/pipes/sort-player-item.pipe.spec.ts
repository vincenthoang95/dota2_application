import { SortPlayerItemPipe } from './sort-player-item.pipe';

describe('SortPlayerItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPlayerItemPipe();
    expect(pipe).toBeTruthy();
  });
});
