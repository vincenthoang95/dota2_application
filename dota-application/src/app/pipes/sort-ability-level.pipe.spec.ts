import { SortAbilityLevelPipe } from './sort-ability-level.pipe';

describe('SortAbilityLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAbilityLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
